# Perfil LOCAL: comodidades de desarrollo. Jamas usar en ambientes compartidos.
spring:
  jpa:
    show-sql: true
    properties:
      "[hibernate.format_sql]": true
logging:
  level:
    "[cl.smid.auth]": DEBUG
